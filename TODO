- test (anything else?)
- bugs
  - picks the last text node as default name
  - disabled is sometimes white bg?
  - when text wraps 
    - make the actual text area width:9999px and scroll on overflow?
    - remove line numbers
- refactor
  - split into smaller components
  - delete unused code
  - lint?
  - comment
- rename to "Repeat Grid Text Editor"
- publish adobe-xd-types to npm and import 